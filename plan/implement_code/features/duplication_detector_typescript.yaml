urn: "feature:implement-code:duplication-detector-typescript"
wagon: "wagon:implement-code"
description: "Intra-layer TypeScript code duplication detection via regex-based structural normalization"
sizing:
  wmbts: 1
  footprint_score: 3
  footprint_size: "S"
wmbts:
  - "wmbt:implement-code:D005"
components:
  backend:
    presentation:
      - type: "controllers"
        count: 0
        rationale: "No CLI changes — plugs into existing atdd validate coder"
    application:
      - type: "use_cases"
        count: 0
        rationale: "Self-contained validator, no orchestration needed"
    domain:
      - type: "entities"
        count: 0
        rationale: "Regex-based analysis, no domain entities"
    integration:
      - type: "repositories"
        count: 0
        rationale: "No persistence — reads source files directly"
