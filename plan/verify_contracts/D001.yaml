urn: "wmbt:verify-contracts:D001"
step: "define"
direction: "maximize"
dimension: "likelihood"
object_of_control: "error-response-convention-completeness"
context_clarifier: "when error handling rules must be documented before validator enforcement"
lens: "functional.sustainability"
statement: "maximize likelihood of error-response-convention-completeness when error handling rules must be documented before validator enforcement"
acceptances:
  - identity:
      urn: "acc:verify-contracts:D001-UNIT-001-convention-exists"
      id: "AC-UNIT-001"
      purpose: "Error-response convention YAML exists with required sections"
      phase: "GREEN"
    harness:
      type: "unit"
      category: "backend"
    given:
      abstract:
        - "The error-response.convention.yaml file is expected at src/atdd/coder/conventions/"
    when:
      abstract: "Checking for convention file existence and structure"
    then:
      abstract:
        - "File exists and is valid YAML with schema_version, convention_id, rules, error_code_format, phase_guidance"
    metadata:
      author: "atdd:verify-contracts"
      created: "2026-03-31"
